import colors from './colors';
import { breakpoints, spacing, maxWidths } from './breakpoints';
export * from './breakpoints';
import typography from './typography';

export default {
  breakpoints,
  colors,
  maxWidths,
  spacing,
  typography,
};
